// This starter route file is kept only for compatibility with the original sandbox.
// The Lab 2 required routes are implemented directly in index.js:
// /user/list, /user/:id, /photosOfUser/:id, /test/info.
const express = require("express");
const router = express.Router();

module.exports = router;
